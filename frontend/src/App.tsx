/**
 * M1 scope: skeleton shell only. Real dashboard views (model list, drift
 * results, alerts, etc.) are built starting in M11.
 */
export default function App() {
  return (
    <div style={{ fontFamily: "sans-serif", padding: "2rem" }}>
      <h1>ModelWatch</h1>
      <p>Frontend skeleton — dashboard functionality lands in later milestones.</p>
    </div>
  );
}
